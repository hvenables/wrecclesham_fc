- if @videos.any?
  .container
    .well.well-sm
      %h3.title Latest Videos
    .col-xs-12.col-sm-6
      = paginate @videos
    .col-xs-12.col-sm-6
      .page-entries.pull-xs-left.pull-sm-right
        = page_entries_info @videos
    .row
      .card-deck-wrapper
        .card-deck
          - @videos.each do |video|
            = render 'shared/card', news: video, width: 'col-md-4'
- else
  No videos have been added
